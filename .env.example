# Copy to .env and fill in real values. Never commit .env.

# Path to the SQLite database file
DATABASE_URL=sqlite:///data/job_radar.db

# Optional: API keys for sources that require them
# GREENHOUSE_TOKEN=
# LEVER_TOKEN=
