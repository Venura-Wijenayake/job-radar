DATABASE_PATH=data/job_radar.db
LOG_LEVEL=INFO

# Adzuna API credentials (get free keys at https://developer.adzuna.com)
ADZUNA_APP_ID=your_app_id_here
ADZUNA_APP_KEY=your_app_key_here

# Optional — used by Phase 4.6b industry classifier (hybrid LLM mode).
# Leave commented out / unset for the keyword-only fallback path.
# ANTHROPIC_API_KEY=your_key_here
